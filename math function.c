#include <stdio.h>
#include <math.h>
int main()
{
    double a,b;
    double c;
    scanf("%lf",&a);
    scanf("%lf",&b);
    scanf("%lf",&c);
    printf("%lf\n",pow(a,b));
    printf("%lf",sqrt(c));
    return 0;
}